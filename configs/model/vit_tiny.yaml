# @package _global_
model:
  _target_: graph_attention.models.simple_vit.SimpleViT
  image_size: 32
  patch_size: 4   
  dim: 192        # Standard Tiny embedding dim
  depth: 12       # Standard depth for convergence
  num_heads: 3    # 192 / 3 = 64 head dimension (standard)
  mlp_ratio: 4    # Standard expansion ratio